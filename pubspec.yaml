name: sample_hook_riverpod
description: A new Flutter project for testing hooks_riverpod.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_hooks: ^0.18.0
  freezed_annotation: ^0.14.2
  hooks_riverpod: ^1.0.0-dev.7
  path_drawing: ^0.5.1
  pin_code_fields: ^7.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.1
  pedantic: ^1.11.1
  freezed: ^0.14.2

flutter:
  uses-material-design: true